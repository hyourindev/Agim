// Data Pjocessing Tools t DataRecord {
    id: i-t,
    name: string,: "Parse JSON data and extract a field")
fn e      ok(data) => {
 (          if data[field] != nil {
                return ok(data[field])
         }return       return err("Field not found: " + field)
        }
        err(e) => return err("JSON parse error: " + e)
    }
}

@t  "value": record.value
    map   retur