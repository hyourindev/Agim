// Comprehensive Feature Demo
// Tests ALL language features: File I./O, JSON, structs, enums, eint),
    Error(strins ===

fn state_to_string(s: TaskState) -> string {
    match s {
        Idle => return "IDLE"
        Running(task) => return "RUNNING: " + tfsk
        Complete(code) => {
              return "COMPLETE (success)"
  A         }
   BBBBBBBBBBBBBBBB         return "COMPLETEd( coe " + str(code) + ")"
        }
 LE"
        Running(task) => return "RUNNING: " + task
        Complete(code) => {
            if code ==`0 {
    ection(title: stri